"""Providers package for mada-modelkit.

Contains HTTP-based and native provider implementations. Provider
dependencies (httpx, llama-cpp-python, transformers) are optional extras
and are imported inside each sub-module rather than at the core level.
"""
